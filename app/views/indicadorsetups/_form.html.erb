<%= form_for(@indicadorsetup) do |f| %>
  <% if @indicadorsetup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@indicadorsetup.errors.count, "error") %> prohibited this indicadorsetup from being saved:</h2>

      <ul>
      <% @indicadorsetup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :indicador_id %><br>
    <%= f.text_field :indicador_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
