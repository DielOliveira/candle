<%= form_for(@titulo) do |f| %>
  <% if @titulo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@titulo.errors.count, "error") %> prohibited this titulo from being saved:</h2>

      <ul>
      <% @titulo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo %><br>
    <%= f.text_field :codigo %>
  </div>
  <div class="field">
    <%= f.label :valortick %><br>
    <%= f.text_field :valortick %>
  </div>
  <div class="field">
    <%= f.label :flagativo %><br>
    <%= f.check_box :flagativo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
