
  <%= form_for(@pregoestitulosgrafico) do |f| %>

  <% if @pregoestitulosgrafico.errors.any? %>
  <div class="alert alert-danger alert-dismissible fade in" role="alert">
    <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>

    <% if @pregoestitulosgrafico.errors.any? %>

    <% @pregoestitulosgrafico.errors.full_messages.each do |msg| %>
    <%= msg %></br>
    <% end %>
    <% end %>
  </div>
  <% end %>   

<div class="box-body">
    <div class="form-group">
      <%= f.hidden_field :pregoestitulo_id, :class => "form-control", :value => @pregoestitulo.id %>
    </div>
    <div class="form-group">
      <%= f.label :arquivo_id %>
      <%= f.file_field :arquivo_id, :class => "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :Tempo_Gráfico %>
      <%= f.select :tempografico_id, options_from_collection_for_select(Tempografico.all, 'id', 'tempo', @pregoestitulosgrafico.tempografico_id), {:include_blank => false}, {:class => 'form-control' } %>      
    </div>
  </div>
  <!-- /.box-body -->
  <div class="box-footer">
    <button type="submit" class="btn btn-info pull-right">Salvar</button>
  </div>

<% end %>
