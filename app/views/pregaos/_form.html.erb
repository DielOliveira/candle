<%= form_for(@pregao) do |f| %>
  <% if @pregao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pregao.errors.count, "error") %> prohibited this pregao from being saved:</h2>

      <ul>
      <% @pregao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data %><br>
    <%= f.date_select :data %>
  </div>
  <div class="field">
    <%= f.label :flagativo %><br>
    <%= f.check_box :flagativo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
